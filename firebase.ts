// Firebase initialization disabled as per user request to focus on frontend/static content.
// Mock service is used instead of Firebase.

export const auth = {} as any;
export const db = {} as any;

const app = {} as any;
export default app;
